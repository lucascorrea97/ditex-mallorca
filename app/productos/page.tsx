import { PagePlaceholder } from "@/components/site/page-placeholder";

export const metadata = { title: "Productos — D.TEX Mallorca" };

export default function Page() {
  return <PagePlaceholder title="Productos" />;
}
